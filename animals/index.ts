let animals = ["Dog", "Cat", "Fish"];

// back to using normal ForEach loop
animals.forEach((animal) => {
    console.log(`${animal} Would be a great pet or gift to someone!`);
})
console.log("These animals are all housepets thats what they have in common")
